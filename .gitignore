# 가상환경 폴더 무시 (가장 중요)
venv/
.venv/

# 보안 파일 무시 (절대 올리면 안 됨)
.env
config/secrets.json
secrets.toml
.streamlit/secrets.toml

# 파이썬 캐시 파일 무시
__pycache__/
*.pyc

# 로컬 학습 로그 (학생 개인정보 포함, Streamlit Cloud에서 재생성됨)
study_logs.csv

# Claude Code 로컬 메모리
.claude/

# 한글 파일명 JS 파일
*.js